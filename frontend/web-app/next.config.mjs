/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'img1.oto.com.vn',
            'cdn.pixabay.com'
        ]
    }
};

export default nextConfig;
